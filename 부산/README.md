# 부산/ — 최종 산출물

```bash
python3 src/pipeline/extract_busan.py     # 부산 추출
python3 src/pipeline/keco_run.py build    # 직종코드 부여
```

| 파일 | 건수 | 설명 |
|---|---:|---|
| **`부산_공고_직종분류.csv`** | **25,135** | ⭐ **최종 결과물.** 아래 통합본 + KECO 직종코드 18컬럼 · 64컬럼 |
| `부산_공고_통합.csv` | 25,135 | 직종코드 부여 전 · 46컬럼 |
| `부산_직종별_집계.csv` | 459 | 세세분류별 건수 |
| `부산_직종대분류_집계.csv` | 10 | 대분류별 건수 |
| `직종분류_정확도.csv` | 2,559 | 정답 세트 건별 채점 결과 |
| `부산_공고_원본.csv` | 57,369 | 병합 전 행 + `부산판정근거`·`부산판정확실도` |
| `부산_사이트별.csv` | 18 | 사이트별 건수 |

**`부산_공고_직종분류.csv` 가 이 프로젝트의 결과물입니다.**
행 하나가 공고 하나이고, 같은 공고가 여러 사이트에 올라온 경우 하나로 합쳐져 있습니다.
원본 46컬럼은 값·순서 그대로 보존됩니다.

## 직종코드확실도로 걸러 쓰세요

| 확실도 | 건수 | 부여 깊이 | 실측 정확도(대분류) |
|---|---:|---|---:|
| **강** | 6,653 | 세세분류(6자리) | **98%** |
| 중 | 655 | 세세분류(6자리) | **98%** |
| 약 | 15,161 | 대분류(1자리)만 | 72% |
| 미분류 | 1,862 | — | — |

정확도는 `직종` 컬럼 2,559건(공식 명칭 완전일치)을 정답으로 두고 5-fold
교차검증한 실측값입니다. 자세한 내용은 [docs/10](../docs/10_직종코드_분류.md).

## 부산판정확실도로 걸러 쓰세요

| 확실도 | 건수 | 근거 |
|---|---:|---|
| **강** | 23,683 | 시도=부산 · 상세 근무지주소가 부산 · 부산 시군구 |
| 중 | 1,414 | 다지역 공고에 부산 포함 · 부산포털인데 지역 미상 |
| 약 | 38 | 제목에만 부산 (근무지주소 없음) |

엄격하게 쓰려면 `부산판정확실도 == "강"` 만 쓰면 됩니다.

> `부산_판정보류.csv` 는 더 이상 만들지 않습니다.
> "제목엔 부산인데 근무지주소는 타지역" 인 행을 담으려 했는데, 실제로 0건입니다.
> 상세 근무지주소가 있는 공고는 이미 앞 단계 판정을 통과하기 때문입니다.

## 컬럼 46개

| 묶음 | 컬럼 |
|---|---|
| 직무 | `직무` `직무상세` `직무키워드` `담당업무` `자격요건` `우대사항` `직종` |
| 회사·업종 | `회사명` `업종` `직급직책` |
| 근무조건 | `근무요일` `근무시작` `근무종료` `근무시간` · `급여형태` `급여최소` `급여최대` `급여` |
| 채용조건 | `채용인원` `채용인원구분` `모집인원원문` `학력` `경력구분` `최소연차` `고용형태표준` |
| 위치 | `시도` `시군구` `지역원문` `상세주소` |
| 기간 | `마감일` `마감구분` |
| **원문** | `원본_직무` `원본_경력` `원본_고용형태` `원본_지역` `원본_마감일` |
| 추적 | `통합키` `게재사이트수` `게재사이트` `대표URL` `전체URL` `수집시각` |
| 판정 | `부산판정근거` `부산판정확실도` |

**가공값과 원문을 함께 싣습니다.** `직무` 는 6개 출처를 병합한 토큰이라 편하지만,
사이트가 실제로 뭐라고 적었는지는 `원본_직무` 로 확인해야 합니다.

→ [../docs/09_데이터_설명_팀공유.md](../docs/09_데이터_설명_팀공유.md)

전체 스키마는 [../docs/05_데이터_사전.md](../docs/05_데이터_사전.md),
**쓰기 전 주의사항은 [../docs/06_알려진_한계.md](../docs/06_알려진_한계.md) 필독.**
